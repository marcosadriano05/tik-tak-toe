{
  "devDependencies": {
    "@types/node": "^20.5.9",
    "vite": "^4.4.9",
    "vitest": "^0.34.3"
  },
  "scripts": {
    "test": "vitest",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "chmod +x ./deploy.sh && ./deploy.sh"
  }
}

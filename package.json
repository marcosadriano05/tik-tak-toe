{
  "devDependencies": {
    "@types/node": "^17.0.42",
    "vite": "^2.9.12",
    "vitest": "^0.15.0"
  },
  "scripts": {
    "test": "vitest",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "chmod +x ./deploy.sh && ./deploy.sh"
  }
}
